# web/ — Frontend_UI (Person B)

Vercel-served browser UI: search + Scan control, verdict card, and gallery.

Implemented in later tasks (13–15). See `.kiro/specs/packguard/design.md`
(`Frontend_UI`) and `tasks.md`.
